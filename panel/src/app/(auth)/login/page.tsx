import { LoginCard } from "@/components/auth/login-card";

const LoginPage = async () => {
    return (
        <div className="dark flex justify-center items-center min-h-screen">
            <LoginCard />
        </div>
    )
}

export default LoginPage